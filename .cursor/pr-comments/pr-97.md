# Relatório de Comentários do PR #97

**Título**: feat: histórico de commits com filtro de cards por branch no projeto
**URL**: https://github.com/LuizBertucci/10xdev/pull/97
**Branch**: fix/projeto-id
**Gerado em**: 2026-03-03 UTC

---

## Estatísticas

- Total de comentários inline: 20
- Fora do diff (outside diff): 2
- Actionable comments postados: 17
- Total de reviews: 1 (CodeRabbit)
- Autores: coderabbitai[bot]

---

## 🔴 Critical (2)

### 1. `upsertMappingsBulk` — conflict key quebra isolamento de branch

**Arquivo**: [backend/src/models/GithubModel.ts:104](backend/src/models/GithubModel.ts#L104)
**Tipo**: Potential issue

O conflito está em `project_id,file_path` apenas. Para o mesmo path em branches diferentes, um mapping sobrescreve o outro.

```diff
-        .upsert(mappingsToUpsert, { onConflict: 'project_id,file_path' })
+        .upsert(mappingsToUpsert, { onConflict: 'project_id,file_path,branch_name' })
```

---

### 2. SQL Injection em `getCardsByBranch`

**Arquivo**: [backend/src/models/ProjectModel.ts:949](backend/src/models/ProjectModel.ts#L949)
**Tipo**: Potential issue — Security

Interpolação direta do parâmetro `branch` na query SQL. Mesmo padrão do `getCards`.

```diff
       if (branch) {
-        query = query.or(`branch_name.eq."${branch}",branch_name.is.null`)
+        query = query.or(`branch_name.eq.${branch},branch_name.is.null`)
         // ou usar .filter() com binding seguro
       }
```

---

## 🟠 Major (9)

### 3. Branch `'main'` hardcoded no import/connect do GitHub App

**Arquivo**: [backend/src/controllers/ProjectController.ts:177](backend/src/controllers/ProjectController.ts#L177) (também linha 157)
**Tipo**: Potential issue

Repositórios com branch padrão diferente (`master`, `develop`) podem importar a árvore errada ou falhar.

---

### 4. Resultado de `deleteByProject` ignorado no disconnect

**Arquivo**: [backend/src/controllers/ProjectController.ts:769](backend/src/controllers/ProjectController.ts#L769)
**Tipo**: Potential issue

Se `GithubModel.deleteByProject(id)` falhar, a API retorna sucesso e deixa mappings órfãos.

```diff
-    await GithubModel.deleteByProject(id)
+    const deleteResult = await GithubModel.deleteByProject(id)
+    if (deleteResult.error) {
+      return res.status(500).json({ error: 'Falha ao limpar mappings do projeto' })
+    }
```

---

### 5. ProjectModel linha 882 — análise incompleta

**Arquivo**: [backend/src/models/ProjectModel.ts:882](backend/src/models/ProjectModel.ts#L882)
**Tipo**: Potential issue

CodeRabbit identificou potencial problema nessa linha (análise encadeada). Verificar manualmente o contexto das linhas 870–890.

---

### 6. Erro de `removeCardsByBranch` ignorado silenciosamente

**Arquivo**: [backend/src/services/githubService.ts:819](backend/src/services/githubService.ts#L819)
**Tipo**: Potential issue

`removeCardsByBranch` retorna `void` e erros são ignorados. Falha parcial pode criar duplicatas ou dados órfãos na importação subsequente.

---

### 7. Injeção de query na URL de import-github-token

**Arquivo**: [frontend/app/import-github-token/page.tsx:83](frontend/app/import-github-token/page.tsx#L83)
**Tipo**: Potential issue — Security

`installationId` vem da query string e está sendo interpolado diretamente na URL.

```diff
-    const url = `/api/github/connect?installation_id=${installationId}&project_id=${projectId}`
+    const params = new URLSearchParams({ installation_id: installationId, project_id: projectId })
+    const url = `/api/github/connect?${params}`
```

---

### 8. `screen.route` ausente no mapeamento fallback de commit para card (frontend)

**Arquivo**: [frontend/pages/ProjectDetail.tsx:1124](frontend/pages/ProjectDetail.tsx#L1124)
**Tipo**: Potential issue

O map indexa só `block.route`. Cards que dependem de `screen.route` desaparecem no filtro de commit.

```diff
+    // Adicionar screen.route ao índice
+    if (screen.route) routeToCardId.set(screen.route, card.id)
     for (const block of screen.blocks ?? []) {
       if (block.route) routeToCardId.set(block.route, card.id)
     }
```

---

### 9. Files de diff não repassados para cards matched via fallback (frontend)

**Arquivo**: [frontend/pages/ProjectDetail.tsx:1719](frontend/pages/ProjectDetail.tsx#L1719)
**Tipo**: Potential issue

O filtro só repassa files onde `f.card?.id === cardFeature.id`. Cards matched por route fallback aparecem na lista mas sem payload de diff.

---

### 10. `findCardByFilePath` ignora `screen.route` no fallback (backend)

**Arquivo**: [backend/src/models/ProjectModel.ts:1044](backend/src/models/ProjectModel.ts#L1044)
**Tipo**: Potential issue

Mesmo problema do item 8, no lado do backend. Só `block.route` é verificado.

```diff
       for (const screen of screens) {
+        if (screen.route && filePath.includes(screen.route)) return screen.card
         for (const block of screen.blocks ?? []) {
           if (block.route && filePath.includes(block.route)) return block.card
         }
       }
```

---

### 11. Cleanup de branch não é atômico (sem transaction)

**Arquivo**: [backend/src/models/ProjectModel.ts:1286](backend/src/models/ProjectModel.ts#L1286)
**Tipo**: Potential issue

Muta `github_file_mappings`, `project_cards` e `card_features` sem transação. Falha intermediária deixa dados inconsistentes.

> Conforme guidelines: usar `supabaseAdmin` para server-side com transaction quando disponível.

---

## 🟡 Minor (9)

### 12. Env de timeout sem fallback para nome novo

**Arquivo**: [backend/src/server.ts:274](backend/src/server.ts#L274)

A rota mudou para `github`, mas o timeout ainda depende só de `GITSYNC_CONNECT_TIMEOUT_MS`. Se a infra migrou para `GITHUB_CONNECT_TIMEOUT_MS`, o valor configurado será ignorado.

---

### 13. Type assertion insegura para status de arquivo do commit

**Arquivo**: [backend/src/services/githubService.ts:661](backend/src/services/githubService.ts#L661)

`status` é cast direto sem validação. Status inesperado do GitHub (ex: `'copied'`, `'changed'`) passa sem verificação.

---

### 14. Ícone de share sem nome acessível

**Arquivo**: [frontend/components/CardFeatureCompact.tsx:552](frontend/components/CardFeatureCompact.tsx#L552)

Ação de ícone sem label acessível (`aria-label`). Não usa o primitivo de botão compartilhado do componente.

---

### 15. `sessionStorage` sem guard no hydration do OAuth

**Arquivo**: [frontend/components/ProjectForm.tsx:60](frontend/components/ProjectForm.tsx#L60)

`sessionStorage.getItem(...)` pode lançar exceção e abortar o effect todo, pulando hydration mesmo com `installation_id` na URL.

---

### 16. Sem fallback para chave `gitsync` no middleware

**Arquivo**: [frontend/middleware.ts:169](frontend/middleware.ts#L169)

Com a troca direta para `github_sync`, retornos antigos com `gitsync` podem cair no redirect para login indevidamente durante a migração.

---

### 17. `handleImportBranch` sem tratamento de erro (frontend)

**Arquivo**: [frontend/pages/ProjectDetail.tsx:527](frontend/pages/ProjectDetail.tsx#L527)

Sem `catch`; quando a request rejeita, o usuário não recebe feedback algum.

```diff
   const handleImportBranch = async (branch: string) => {
     try {
       await importBranch(branch)
+    } catch (err) {
+      console.error('Erro ao importar branch:', err)
+      toast.error('Falha ao importar branch')
     } finally {
       setImportingBranch(false)
     }
   }
```

---

### 18. `loadCommits` e `handleCommitSelect` não surfaceiam erros

**Arquivo**: [frontend/pages/ProjectDetail.tsx:557](frontend/pages/ProjectDetail.tsx#L557)

Requests com falha parecem resultados vazios em vez de erros acionáveis.

---

### 19. Contadores de mapping coverage incorretos para `undefined`

**Arquivo**: [backend/src/controllers/ProjectController.ts:637](backend/src/controllers/ProjectController.ts#L637)

`file.card !== null` considera `undefined` como mapeado. Usar `Boolean(file.card?.id)`.

---

### 20. `ref` ausente ao buscar conteúdo de arquivo no resolve de conflito

**Arquivo**: [backend/src/services/githubService.ts:1163](backend/src/services/githubService.ts#L1163)

Ao resolver conflito com `'keep_github'`, o conteúdo é buscado sem `ref`, pegando HEAD do branch padrão (pode não ser o branch correto).

---

## ⚠️ Outside Diff (2)

### OD-1. Chave legada de redirect não lida no login

**Arquivo**: [frontend/app/login/page.tsx:24-33](frontend/app/login/page.tsx#L24)
**Tipo**: Minor

`getRedirectTarget` só lê `github_sync_redirect_after_login`. Usuários com fluxo iniciado antes do deploy perdem o destino pós-login.

```diff
-      const stored = sessionStorage.getItem('github_sync_redirect_after_login')
+      const stored =
+        sessionStorage.getItem('github_sync_redirect_after_login') ??
+        sessionStorage.getItem('gitsync_redirect_after_login')
       if (stored) {
         sessionStorage.removeItem('github_sync_redirect_after_login')
+        sessionStorage.removeItem('gitsync_redirect_after_login')
         return stored
       }
```

### OD-2. GithubModel — outside diff adicional

**Arquivo**: [backend/src/models/GithubModel.ts](backend/src/models/GithubModel.ts)
**Tipo**: Relacionado ao item #1 (conflict key)

Comentário fora do diff, relacionado ao mesmo problema de isolamento de branch do item crítico #1.

---

## Resumo Prioritário

| # | Sev | Arquivo | Problema |
|---|-----|---------|----------|
| 1 | 🔴 | GithubModel.ts:104 | conflict key quebra branch isolation |
| 2 | 🔴 | ProjectModel.ts:949 | SQL injection em getCardsByBranch |
| 3 | 🟠 | ProjectController.ts:177 | 'main' hardcoded no import |
| 4 | 🟠 | ProjectController.ts:769 | deleteByProject ignorado no disconnect |
| 5 | 🟠 | githubService.ts:819 | removeCardsByBranch sem error handling |
| 6 | 🟠 | import-github-token/page.tsx:83 | URL injection com installationId |
| 7 | 🟠 | ProjectDetail.tsx:1124 | screen.route ausente no mapa fallback |
| 8 | 🟠 | ProjectDetail.tsx:1719 | diff não repassado para cards fallback |
| 9 | 🟠 | ProjectModel.ts:1044 | screen.route ignorado no findCardByFilePath |
| 10 | 🟠 | ProjectModel.ts:1286 | cleanup de branch sem transação |
| 11 | 🟡 | ProjectDetail.tsx:527 | handleImportBranch sem catch |
| 12 | 🟡 | ProjectDetail.tsx:557 | loadCommits sem surface de erro |
| 13 | 🟡 | ProjectController.ts:637 | contador mapping usa !== null em vez de Boolean |
| 14 | 🟡 | githubService.ts:661 | type assertion sem validação |
| 15 | 🟡 | githubService.ts:1163 | ref ausente no fetch de conteúdo |
| 16 | 🟡 | server.ts:274 | env de timeout sem fallback |
| 17 | 🟡 | middleware.ts:169 | sem fallback para chave gitsync |
| 18 | 🟡 | ProjectForm.tsx:60 | sessionStorage sem guard no OAuth |
| 19 | 🟡 | CardFeatureCompact.tsx:552 | share icon sem aria-label |
| OD-1 | 🟡 | login/page.tsx:24 | chave legada redirect não lida |
